services:
  - type: web
    name: diagbot-dashboard
    runtime: docker
    plan: free
    region: frankfurt
    repo: https://github.com/belesalibaba-ui/diagbot-dashboard
    branch: main
    dockerContext: .
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
